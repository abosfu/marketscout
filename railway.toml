[build]
builder = "nixpacks"
buildCommand = "pip install -e ."

[deploy]
startCommand = "uvicorn marketscout.backend.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"

[environment]
NIXPACKS_PYTHON_VERSION = "3.9"
